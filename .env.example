# Server port (default 5303)
PORT=5303

# Admin password for the dashboard (CHANGE THIS)
ADMIN_PASSWORD=changeme

# Where the SQLite database + uploads live (default: ./data)
# DATA_DIR=/path/to/data

# Public base URL used in emailed invoice links (e.g. https://billing.yourdomain.com)
# PUBLIC_BASE_URL=
